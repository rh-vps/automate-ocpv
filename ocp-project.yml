- name: create project
  hosts: all
  gather_facts: no
  ansible_python_interpreter: "/usr/libexec/platform-python"
  tasks:
    - name: Create a k8s namespace
      kubernetes.core.k8s:
        name: testing
        api_version: v1
        kind: Namespace
        state: present
